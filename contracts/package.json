{
  "name": "alias-registry",
  "version": "1.0.0",
  "description": "Contrato AliasRegistry para transacciones privadas sobre Railgun",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:polygon": "hardhat run scripts/deploy.ts --network polygon",
    "deploy:amoy": "hardhat run scripts/deploy.ts --network polygonAmoy",
    "deploy:arbsepolia": "hardhat run scripts/deploy.ts --network arbitrumSepolia",
    "verify": "hardhat verify",
    "interact:polygon": "hardhat run scripts/interact.ts --network polygon"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.10.0",
    "dotenv": "^16.3.0"
  }
}
